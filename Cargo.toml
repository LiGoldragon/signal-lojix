[package]
name         = "signal-lojix"
version      = "0.2.0"
edition      = "2024"
rust-version = "1.88"
license      = "MIT OR Apache-2.0"
description  = "Ordinary Signal contract for the lojix deploy orchestrator component."
publish      = false
links        = "signal-lojix"
build        = "build.rs"

[lib]
name = "signal_lojix"
path = "src/lib.rs"

[features]
default   = []
nota-text = ["dep:nota", "signal-frame/nota-text"]

[dependencies]
nota    = { git = "https://github.com/LiGoldragon/nota.git", branch = "main", optional = true }
rkyv         = { version = "0.8", default-features = false, features = ["std", "bytecheck", "little_endian", "pointer_width_32", "unaligned"] }
signal-frame = { git = "https://github.com/LiGoldragon/signal-frame.git", branch = "main", default-features = false }

[build-dependencies]
schema-rust = { git = "https://github.com/LiGoldragon/schema-rust.git", branch = "main" }

[lints.rust]
unsafe_code = "forbid"
unused      = { level = "warn", priority = -1 }
dead_code   = "warn"
